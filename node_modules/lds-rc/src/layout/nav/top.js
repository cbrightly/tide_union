import React, { Component } from 'react';
import PropTypes from 'prop-types';
import LayoutNavBase from './base';

export default class LayoutNavTop extends LayoutNavBase {
  constructor(props) {
  	let options = {
  		navStyle: Object.assign({
  			top: "0"
  		}, props.navStyle),
  		navLinkStyle: Object.assign({
  			
  		}, props.navLinkStyle)
  	};
  	super(props, options);
	}
}

LayoutNavTop.propTypes = {
	
}