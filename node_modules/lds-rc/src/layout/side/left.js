import React, { Component } from 'react';
import PropTypes from 'prop-types';
import LayoutSideBase from './base';

export default class LayoutSideLeft extends LayoutSideBase {
  constructor(props) {
  	super(props, {
  		panelStyle: Object.assign({
  			top: "0",
  			left: "0",
	  		background: "rgba(0,0,0,.88)",
	  		width: "80vw",
	  		height: "100vh"
  		}, props.panelStyle),
	  	backdropStyle: Object.assign({
	  		background: "rgba(0,0,0,.2)"
	  	}, props.backdropStyle)
  	});
  	
    this.direction = {
    	"PUSH": "left",
    	"POP": "left"
    };
	}
}

LayoutSideLeft.propTypes = {
	
}